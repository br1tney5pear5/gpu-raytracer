#version 330

//=============================== mainvert
// layout(position = 0) in vec3 position;

out gl_PerVertex { vec4 gl_Position; };

void main() {
    // gl_Position = vec4(1.0,1.0,1.0,1.0);
}
